<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KProfiles" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary_light</item>
        <item name="colorPrimaryVariant">@color/colorPrimaryDark_light</item>
        <item name="colorOnPrimary">@color/colorAccent_light</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" >?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="android:windowShowWallpaper">true</item>
        <item name="android:textColor">@color/textColor</item>
        <item name="buttonStyle">@style/Theme.ButtonStyle</item>
        <item name="materialButtonStyle">@style/Theme.ButtonStyle</item>
    </style>

    <style name="Theme.ButtonStyle" parent="Widget.MaterialComponents.Button">
        <item name="android:textColor">@color/textColor</item>
    </style>
</resources>